import { EmpTitlePipePipe } from './emp-title-pipe.pipe';

describe('EmpTitlePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmpTitlePipePipe();
    expect(pipe).toBeTruthy();
  });
});
